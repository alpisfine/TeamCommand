var utility = {
registeredPlugins:[]
}

utility.help = function(teamspeak){ // A !help command for the listings of commands.

    this.registeredPlugins.forEach((plugin)=>{

});
var messageParts = []; // Ts3 defines a maximum of 1024 bytes per text message. So i would slice the overall message to parts.
}

module.exports = utility;